<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Публичный URL медиа (поддомен img.*)
    |--------------------------------------------------------------------------
    | Задаётся через MEDIA_URL. Используется для генерации ссылок на файлы
    | и (если хост отличается от APP_URL) для маршрута отдачи с S3.
    */
    'url' => env('MEDIA_URL'),

    'host' => ($host = parse_url((string) env('MEDIA_URL', ''), PHP_URL_HOST)) ? $host : null,

    'prefix' => env('MEDIA_PREFIX', ''),

];
